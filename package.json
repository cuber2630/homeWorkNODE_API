{
  "name": "chnbase",
  "version": "1.0.0",
  "description": "",
  "main": "chnApi.js",
  "scripts": {
    "start": "node chnApi.js",
    "build:debug": "node-gyp configure && node-gyp build --debug",
    "build:release": "node-gyp configure && node-gyp build",
    "clean": "node-gyp clean"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^2.0.0"
  },
  "devDependencies": {
    "node-gyp": "^6.1.0"
  }
}
